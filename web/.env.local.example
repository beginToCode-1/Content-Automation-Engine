# URL of the deployed Content Automation Engine FastAPI backend (Railway/Render).
# Falls back to http://localhost:8000 if unset, for local development against
# a backend started with `uvicorn` on its default port.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
